from datetime import date

class Horse:

    def __init__(self, name, species, walking):
        self.name = name
        self.species = species
        self.walking = walking
        self.date_added = date.today()

class Dog:

    def __init__(self, name, species, walking):
        self.name = name
        self.species = species
        self.walking = walking
        self.date_added = date.today()

class Cat:

    def __init__(self, name, species, walking):
        self.name = name
        self.species = species
        self.walking = walking
        self.date_added = date.today()

class Squirrel:

    def __init__(self, name, species, walking):
        self.name = name
        self.species = species
        self.walking = walking
        self.date_added = date.today()

class Groundhog:

    def __init__(self, name, species, walking):
        self.name = name
        self.species = species
        self.walking = walking
        self.date_added = date.today()

class Duck:

    def __init__(self, name, species, swimming):
        self.name = name
        self.species = species
        self.swimming = swimming
        self.date_added = date.today()

class Flamingo:

    def __init__(self, name, species, swimming):
        self.name = name
        self.species = species
        self.swimming = swimming
        self.date_added = date.today()

class Goose:

    def __init__(self, name, species, swimming):
        self.name = name
        self.species = species
        self.swimming = swimming
        self.date_added = date.today()

class Loon:

    def __init__(self, name, species, swimming):
        self.name = name
        self.species = species
        self.swimming = swimming
        self.date_added = date.today()

class Goldfish:

    def __init__(self, name, species, swimming):
        self.name = name
        self.species = species
        self.swimming = swimming
        self.date_added = date.today()

class Snake:

    def __init__(self, name, species, slithering):
        self.name = name
        self.species = species
        self.slithering = slithering
        self.date_added = date.today()

class Lizard:

    def __init__(self, name, species, walking):
        self.name = name
        self.species = species
        self.walking = walking
        self.date_added = date.today()

class Spider:

    def __init__(self, name, species, walking):
        self.name = name
        self.species = species
        self.walking = walking
        self.date_added = date.today()

class Toad:

    def __init__(self, name, species, hopping):
        self.name = name
        self.species = species
        self.hopping = hopping
        self.date_added = date.today()

class Frog:

    def __init__(self, name, species, hopping):
        self.name = name
        self.species = species
        self.hopping = hopping
        self.date_added = date.today()

malcom = Frog('Malcom', 'Tree', True)
miles = Toad('Miles', 'Sonoran Desert', True)
zane = Spider('Zane', 'Black Widow', True)
adrian = Lizard('Adrian', 'Chameleon', True)
tom = Snake('Tom', 'Copperhead', True)
leigha = Goldfish('Leigha', 'Gold Goldfish', True)
bryan = Loon('Bryan', 'Northern', True)
joe = Goose('Joe', 'Canadian', True)
sarah = Flamingo('Sarah', 'Purple', True)
luke = Duck('Luke', 'Mallard', True)
eli = Groundhog('Eli', 'Nusance', True)
kaleb = Squirrel('Kaleb', 'Grey', True)
michelle = Cat('Michelle', 'Mountain Lion', True)
brian = Dog('Brian', 'Corgi', True)
kirk = Horse('Kirk', 'Thoroughbred', True)
